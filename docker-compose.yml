services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: meridian
      POSTGRES_PASSWORD: meridian
      POSTGRES_DB: meridian_his
    ports:
      - "5432:5432"
    volumes:
      - meridian_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U meridian -d meridian_his"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  meridian_pg:
